import { FC } from 'react'

interface ServerBodyProps {

}

const ServerBody: FC<ServerBodyProps> = ({ }) => {
    return (
        <div className='flex-grow'>
            ServerBody
        </div>
    )
}

export default ServerBody